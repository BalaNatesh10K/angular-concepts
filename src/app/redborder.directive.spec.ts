import { RedborderDirective } from './redborder.directive';

describe('RedborderDirective', () => {
  it('should create an instance', () => {
    const directive = new RedborderDirective();
    expect(directive).toBeTruthy();
  });
});
